import { FormatAnsweredPipe } from './format-answered.pipe';

describe('FormatAnsweredPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAnsweredPipe();
    expect(pipe).toBeTruthy();
  });
});
